{
  "name": "@monorepo/common",
  "version": "0.0.0-fixed",
  "description": "Common code and configuration used by all monorepo packages",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "lint": "yarn run -T eslint $INIT_CWD",
    "test": "yarn run -T test $INIT_CWD"
  }
}
