{
  "name": "@openshift/dynamic-plugin-sdk-runtime",
  "version": "0.0.0-alpha1",
  "description": "Allows loading, managing and interpreting plugins at runtime",
  "scripts": {
    "prepack": "yarn build",
    "prepublish": "yarn test",
    "build": "rm -rf dist && tsc --outDir dist",
    "test": "echo 'Write some tests!'",
    "lint": "yarn lint:run --ext .ts,.tsx ."
  },
  "dependencies": {
    "joi": "^17.4.2",
    "lodash-es": "^4.17.21",
    "react": "^17.0.2"
  },
  "devDependencies": {
    "@types/lodash-es": "^4.17.5",
    "@types/node": "^12.20.36",
    "@types/react": "^17.0.37",
    "eslint-plugin-internal": "0.0.0-fixed",
    "typescript": "^4.4.4"
  }
}
