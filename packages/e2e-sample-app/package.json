{
  "name": "@monorepo/e2e-sample-app",
  "version": "0.0.0-fixed",
  "description": "E2E testing of sample app",
  "private": true,
  "scripts": {
    "lint": "yarn run -T eslint $INIT_CWD",
    "test": "yarn run -T test $INIT_CWD",
    "test-e2e": "yarn run -T cypress run",
    "test-e2e-open": "yarn run -T cypress open"
  }
}
