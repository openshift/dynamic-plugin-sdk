{
  "name": "monorepo",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn install && yarn workspaces foreach run install && yarn workspaces foreach run test"
  }
}
